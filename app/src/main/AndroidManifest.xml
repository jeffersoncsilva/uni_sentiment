<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.projetos.redes">

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.Manifest.permission.READ_PHONE_STATE" />
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission
        android:name="android.permission.READ_PRIVILEGED_PHONE_STATE"
        tools:ignore="ProtectedPermissions" />

    <application
        android:allowBackup="false"
        android:icon="@drawable/icone_low"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icone_low"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.AjudaActivity"></activity>
        <activity
            android:name=".activities.RecebeConversaExportadaActivity"
            android:label="@string/title_activity_recebe_conversa_exportada_whatsapp">
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="*/*" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.UsoDeInternetActivity"
            android:parentActivityName=".activities.UsoDeInternetActivity" />
        <activity
            android:name=".activities.LexicoProcessadoActivity"
            android:parentActivityName=".activities.MainActivity" />
        <activity
            android:name=".activities.ResultadoFinalActivity"
            android:parentActivityName=".activities.MainActivity" />
    </application>

</manifest>